; Raise upload limits (cPanel default is often only 2M, which rejects
; many phone photos). Applies on PHP-FPM/FastCGI; harmless otherwise.
upload_max_filesize = 8M
post_max_size = 12M
max_file_uploads = 20
